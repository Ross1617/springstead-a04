package baseline;
import java.util.Scanner;
public class productSearch {
    private static final Scanner in = new Scanner(System.in);
    String searchTerm ;

    private void readInput(String prompt){
        //prompts the user for a string
        //returns the user next line
    }

    public int search(){
        //create a new instance of Json

        //use a try thing to open the document

            // parse the data to see if the input is equal to any of the names
            //done by doing a for loop for terms in the input file
            //if the input is not there
                // print a statement saying it is not here and return 0
            //else get all the data
            // save name price and quantity
            // print them out
            // return 1
        //use a catch loop so it will run

    }

}
